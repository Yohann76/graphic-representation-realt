// get data from eth wallet (get all property + amount for this wallet)
export const fetchInfoForOneWallet = async (address) => {
  try {
    // TODO
  } catch (error) {
    console.error("Erreur lors de la récupération des données :", error);
    throw error;
  }
};

// merge data from different eth wallet (return all property + amount)
export const mergeDifferentWallet = async (addressArray) => {
  try {
    // TODO
  } catch (error) {
    console.error("Erreur lors de la récupération des données :", error);
    throw error;
  }
};
